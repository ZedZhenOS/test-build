name: build

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  call-workflow-passing-data:
    uses: ZedZhenOS/workflows/.github/workflows/release_build.yml@main
    with:
      tag_name: github.event.release.tag_name
    secrets: inherit
